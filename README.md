# 📱 迈捷手机内部流程管理系统

一个功能完善的手机维修店工单管理系统，支持工单登记、状态跟踪、价格管理、客户管理等功能。

## ✨ 主要功能

### 📝 工单登记
- 客户信息录入（姓名、电话）
- 设备信息录入（品牌、型号、密码、图案锁）
- 是否需要订货选项（自动设置初始状态）
- 智能选择维修项目（根据品牌型号自动显示可用维修项目）
- 故障描述自动填充
- 预估价格自动计算
- 工单打印

### 📺 前台看板
- 看板式工单状态展示
- 支持拖拽更改工单状态
- 实时搜索工单
- 工单详情查看与打印
- 已完成工单自动隐藏（仅显示3天内）

### 🔧 维修看板
- 5种工单状态：等待订货 → 待维修 → 维修中 → 待取机 → 已完成
- 支持拖拽更改工单状态
- 故障描述显示
- 维修备注记录
- 设备密码/图案锁查看

### 💰 价格管理
- 按品牌、型号、维修类型管理价格
- 支持意大利语名称（双语显示）
- 批量添加维修项目
- 筛选查看（品牌、型号、维修类型）

### 📋 历史记录查询
- 多条件筛选（关键词、状态、品牌、日期）
- 工单详情查看与打印
- 完整历史记录保留

### 👥 客户管理
- 客户信息管理
- 历史工单查看
- 电话搜索客户

## 🛠️ 技术栈

- **后端**: Node.js + Express
- **数据库**: SQLite (better-sqlite3)
- **前端**: 原生 HTML/CSS/JavaScript
- **容器化**: Docker + Docker Compose

## 🚀 快速开始

### 使用 Docker（推荐）

```bash
# 克隆项目
git clone <repository-url>
cd phonemanger

# 启动服务
docker-compose up -d

# 访问系统
open http://localhost:3000
```

### 本地开发

```bash
# 安装依赖
npm install

# 启动服务
npm start

# 访问系统
open http://localhost:3000
```

## 📁 项目结构

```
phonemanger/
├── backend/                 # 后端代码
│   ├── server.js           # 主服务入口
│   ├── database.js         # 数据库初始化
│   └── routes/             # API 路由
│       ├── orders.js       # 工单 API
│       ├── customers.js    # 客户 API
│       ├── pricing.js      # 价格 API
│       └── stats.js        # 统计 API
├── frontend/               # 前端页面
│   ├── index.html         # 工作台首页
│   ├── register.html      # 工单登记
│   ├── front-desk.html    # 前台看板
│   ├── repair.html        # 维修看板
│   ├── history.html       # 历史查询
│   ├── pricing.html       # 价格管理
│   ├── customers.html     # 客户管理
│   ├── css/style.css      # 样式文件
│   └── js/                # JavaScript
│       ├── api.js         # API 客户端
│       └── app.js         # 通用工具
├── data/                   # 数据目录
│   └── shop.db            # SQLite 数据库
├── Dockerfile             # Docker 配置
├── docker-compose.yml     # Docker Compose 配置
└── package.json           # 项目配置
```

## 📊 数据库结构

### customers 客户表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| name | TEXT | 客户姓名 |
| phone | TEXT | 电话号码 |
| created_at | DATETIME | 创建时间 |

### orders 工单表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| order_no | TEXT | 工单号 |
| customer_id | INTEGER | 客户ID |
| device_brand | TEXT | 设备品牌 |
| device_model | TEXT | 设备型号 |
| device_password | TEXT | 设备密码 |
| pattern_password | TEXT | 图案密码 |
| device_power_on | TEXT | 是否开机 |
| problem | TEXT | 故障描述 |
| status | TEXT | 工单状态 |
| estimated_price | REAL | 预估价格 |
| final_price | REAL | 最终价格 |
| deposit | REAL | 押金 |
| repair_notes | TEXT | 维修备注 |
| estimated_date | TEXT | 预计完成日期 |
| created_at | DATETIME | 创建时间 |

### pricing 价格表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER | 主键 |
| brand | TEXT | 品牌 |
| model | TEXT | 型号 |
| category | TEXT | 维修类型 |
| name | TEXT | 项目名称 |
| name_it | TEXT | 意大利语名称 |
| price | REAL | 价格 |
| created_at | DATETIME | 创建时间 |

## 🔗 API 端点

### 工单 API
- `GET /api/orders` - 获取所有工单
- `GET /api/orders/:id` - 获取单个工单
- `POST /api/orders` - 创建工单
- `PUT /api/orders/:id` - 更新工单
- `PUT /api/orders/:id/status` - 更新工单状态

### 价格 API
- `GET /api/pricing` - 获取所有价格
- `GET /api/pricing/brands` - 获取所有品牌
- `GET /api/pricing/models/:brand` - 获取品牌下的型号
- `GET /api/pricing/items/:brand/:model` - 获取品牌型号的维修项目
- `POST /api/pricing` - 添加价格项目
- `PUT /api/pricing/:id` - 更新价格项目
- `DELETE /api/pricing/:id` - 删除价格项目

### 客户 API
- `GET /api/customers` - 获取所有客户
- `GET /api/customers/phone/:phone` - 按电话查询客户

### 统计 API
- `GET /api/stats` - 获取统计数据

## 📝 License

MIT
